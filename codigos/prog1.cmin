void main(void)
{
    int vet[10];
    int i;
    int j;
    int n;
    int sum;
    sum = 0;
    n = 10;

    while(i < 10)
    {
        vet[i] = input();
        i = i + 1;
    }

    while(j < 10)
    {
        sum = sum + vet[i];
        j = j + 1;
    }

    output(sum);
}